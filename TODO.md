# TODO: Fix Time Zone Handling in Reminders

## Tasks
- [x] Fix notification scheduling in `lib/providers/reminder_provider.dart` to convert UTC to local time before scheduling.
- [x] Fix time display in `lib/screens/reminderscreen.dart` to show local time instead of UTC.
- [ ] Test time selection and notification scheduling after fixes.
